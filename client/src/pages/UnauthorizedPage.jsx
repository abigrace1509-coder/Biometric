const UnauthorizedPage = () => (
  <div className="container">
    <div className="alert alert-danger">Unauthorized access. Please login with the correct role.</div>
  </div>
);

export default UnauthorizedPage;
